{
  "name": "cms-ts",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "bun --watch ./src/index.ts",
    "build": "bun run typecheck && bun build ./src/index.ts --minify --outdir ./dist",
    "start": "bun ./dist/index.js",
    "clean": "rm -rf dist && echo 'Cartella dist eliminata.'"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}